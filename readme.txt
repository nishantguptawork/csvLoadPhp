STEPS TO RUN SERVER :
    1. Install wamp apache for your operating system.
    2. Host the application on the server and start the application server instance.
    3. navigate to localhost/<applciationName> on the borwser and enter amount to get the desired result.

NOTE :
    The code is working as expected and has been implemented in PHP and html.